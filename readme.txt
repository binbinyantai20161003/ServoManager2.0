2016.05.21

1:增加了驱动器的指令接口，并增加测试工程进行测试。

2016.6.1

1:修改了指令接口为多线程保护，一次只能有一条指令被执行
2：增加了画图功能，并自己建立多媒体定时器进行测试，上层采用线程进行测试。没有在多媒体定时器中测试两个dsp的通信问题。

2016.6.2:

1: 用多媒体定时器可以稳定的从FPGA获取数据，上层用线程去读取数据。并测试了两个dsp同时读取数据。每个dsp同时通信40个short是没有问题的

2016.6.8：

1：整理了驱动器的指令接口，将接口都具有了物理意义，并且基本都是按照比率进行设置，增加了读空FPGA的指令
2：画图功能进行了整理，接口进一步简化。多媒体定时器改为只启动一次，程序结束时再关闭。
3：该版本的扫描头文件部分还没有进一步完善。这个版本只是作为之前完成功能的备份

2016.06.26

1:增加了flash操作的接口

2：修改了网络通信的等待接收部分代码

2016.06.29

1:SCAN部分增加了打印参数宏定义文件

2016.07.03
1：修改了plotwave工程中的代码，之前为分A,B dsp 进行启停和获取数据，现在改为通过轴号参数来判断启动哪个。
2：plotwavetest工程没有同步更新，
3：修复了plotwave中读取第二个dsp的bug

2016.07.27
1：修改了之前参数地址不对的问题。之前因为模块名的地址都是-1，所以之前的参数FRAM地址都是错的。

2016.12.29
1:该版本之前都是用debug通信的。后面需要加入支持等环网调试的。
